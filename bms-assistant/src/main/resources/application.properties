# Server
server.port=8888
spring.application.name=bms-assistant

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/bms_assistant_db
spring.datasource.username=postgres
spring.datasource.password=74237064f@F

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Deshabilitar seguridad temporalmente para desarrollo
spring.security.user.name=admin
spring.security.user.password=temp123
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Logging para desarrollo
logging.level.com.indra.bms_assistant=DEBUG